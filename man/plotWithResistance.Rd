% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResistance.R
\name{plotWithResistance}
\alias{plotWithResistance}
\title{Show a resistance surface beside a network plot}
\usage{
plotWithResistance(x, gg, maxResistance = 12L)
}
\arguments{
\item{x}{A resistance \code{SpatRaster} shown as the reference panel on the left
(passed to \code{\link[=plotResistance]{plotResistance()}}).}

\item{gg}{A plot shown on the right, e.g. the result of \code{\link[=plot]{plot()}} on an \code{mpg} or
\code{grain} object (typically with \code{theme = FALSE}), or any \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.}

\item{maxResistance}{Passed to \code{\link[=plotResistance]{plotResistance()}}.}
}
\value{
A two-panel plot grid produced by \code{cowplot::plot_grid()}.
}
\description{
Places a resistance-surface reference panel (see \code{\link[=plotResistance]{plotResistance()}}) to the left of a
network plot -- for example one produced by \code{\link[=plot]{plot()}} on an \code{mpg} or \code{grain} object, or
any \pkg{ggplot2} plot -- so the two can be compared at the same size as a visual
sanity check. Both panels are drawn at equal size (using \code{cowplot::plot_grid()} with
\code{align = "h"} and equal coordinates) and the resistance legend is placed at the far
left.

Requires the \pkg{cowplot} package.
}
\examples{
\donttest{
if (requireNamespace("cowplot", quietly = TRUE)) {
  tiny <- terra::rast(
    system.file("extdata", "tiny.asc", package = "grainscape", mustWork = TRUE)
  )
  tinyCost <- terra::classify(tiny, rcl = cbind(c(1, 2, 3, 4), c(1, 5, 10, 12)))
  tinyMPG <- MPG(tinyCost, patch = tinyCost == 1)
  plotWithResistance(tinyCost, plot(tinyMPG, quick = "mpgPlot", theme = FALSE))
}
}

}
\seealso{
\code{\link[=plotResistance]{plotResistance()}}
}
\author{
Alex M Chubaty
}

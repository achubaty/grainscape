% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResistance.R
\name{plotResistance}
\alias{plotResistance}
\title{Plot a resistance surface with a consistent colour scheme}
\usage{
plotResistance(x, maxResistance = 12L)
}
\arguments{
\item{x}{A resistance \code{SpatRaster}, e.g. the \code{cost} surface passed to \code{\link[=MPG]{MPG()}}.}

\item{maxResistance}{Integer. The resistance value mapped to the top (red) end of
the colour ramp; values from \code{1} to \code{maxResistance} receive fixed colours so the
scheme stays consistent across surfaces. Defaults to \code{12}, and is automatically
raised to the largest value present in \code{x} if that exceeds it.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Draws a resistance \code{SpatRaster} using a shared colour scheme (green for low
resistance through red for high resistance) with a discrete-swatch legend placed to
the left of the panel. Each resistance value is mapped to a fixed colour so that the
same value appears identical across figures, which is useful when comparing several
landscapes or when pairing a resistance surface with a network plot (see
\code{\link[=plotWithResistance]{plotWithResistance()}}).
}
\examples{
tiny <- terra::rast(system.file("extdata/tiny.asc", package = "grainscape"))
tinyCost <- terra::classify(tiny, rcl = cbind(c(1, 2, 3, 4), c(1, 5, 10, 12)))
plotResistance(tinyCost)

}
\seealso{
\code{\link[=plotWithResistance]{plotWithResistance()}}, \code{\link[=ggGS]{ggGS()}}
}
\author{
Alex M Chubaty
}
